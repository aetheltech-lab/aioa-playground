# aioa-playground

A tiny Node.js utility library that exists to exercise an automated coding agent end to end
(issue → branch → fix → tests in CI → pull request). No dependencies.

```bash
npm test
```

Contributions via pull request are welcome. Keep changes minimal and covered by tests.
